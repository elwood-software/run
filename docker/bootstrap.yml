cleanup: before
workflow:
  name: "bootstrap"
  jobs:
    default:
      name: "bootstrap"
      steps:
        - action: "install/whisper"
          permissions:
            net: true
            run: true
