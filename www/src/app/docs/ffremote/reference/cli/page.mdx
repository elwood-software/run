export const metadata = {
  title: 'FFremote CLI Reference',
};

# FFremote CLI

The FFremote CLI (Command Line Interface) allows you to run FFMpeg jobs on a remote server. The CLI has a
number of commands that can be used to perform different tasks, check the
links below for more information on each command.

Visit the [Getting Started Guide](/docs/ffremote/start#install) to learn how to install CLI.

## Commands

- [ffremote execute](#execute) - Execute an FFmpeg command on the remote server
- [ffremote list](#list) - List all executions
- [ffremote watch](#watch) - Watch the execution of a command
- [ffremote get](#get) - Download the output of a command
- [ffremote status](#status) - Get the status of a command
- [ffremote auth](#auth) - Authenticate with the remote server
- [ffremote upgrade](#upgrade) - Upgrade the CLI to the latest version

### execute

Execute an FFmpeg command on the remote server

```bash
# Usage
ffremote execute [options] -- <ffmpeg-args>...
ffremote [options] -- <ffmpeg-args>...
ffremote <ffmpeg-args>...

# Examples
ffremote execute -- -i "test.mov" -t 30 -c:v libx264 -c:a aac -strict experimental "test.mp4"
ffremote --size=lg -- -i "test.mov" -t 30 -c:v libx264 -c:a aac -strict experimental "test.mp4"
ffremote -i "test.mov" -t 30 -c:v libx264 -c:a aac -strict experimental "test.mp4"
```

#### Options

##### `--size=<size>`

Size of the remote instance to use. [View available values](/docs/ffremote/pricing)

##### `--include=<include>`

Files to upload to the remote server that aren't used as input (`-i`). You can specify multiple files by repeating the option.

##### `--exclude=<exclude>`

Files to exclude from the final output archive. Input files are automatically excluded from the output. You can specify multiple files by repeating the option. Glob paths are also supported.

### list

List all executions

```bash
# Usage
ffremote list
```

### watch

Watch the execution of a command

```bash
# Usage
ffremote watch [id]

# Examples
ffremote watch 0bb4dce9-1751-4aaa-9b26-fadca6642680
ffremote watch # use last id or prompt
```

### get

Download the output of a command

```bash
# Usage
ffremote get [id]

# Examples
ffremote get 0bb4dce9-1751-4aaa-9b26-fadca6642680
ffremote get # use last id or prompt
```

### status

Get the status of a command

```bash
# Usage
ffremote status [id]

# Examples
ffremote status 0bb4dce9-1751-4aaa-9b26-fadca6642680
ffremote status # use last id or prompt
```

### auth

Authenticate with the remote server

```bash
# Usage
ffremote auth [subcommand]

# Examples
ffremote auth
ffremote auth logout
ffremote auth refresh
```
